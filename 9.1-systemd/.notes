grep '^Download:' deps > deps2
sed 's/Download: */ /' deps2 > deps

cd $LFS/sources -v
unpack 
cd 
./configure --prefix=/tools 
make
make install

pressanykey(){ read -n1 -p "${txtpressanykey}"; }
txtpressanykey="Press any key to continue."
start_time=`date +%s`
sleep 5 && echo $(expr `date +%s` - $start_time) 